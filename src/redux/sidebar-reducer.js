const SIDE_BAR='SIDE-BAR'
const sidebarReducer=(state,action)=>{
    switch (action.type){
        case SIDE_BAR:
            return state;
        default:
            return state;
    }
    return state;
};
export default sidebarReducer;